export default {
    name: "download",
    syntax: "[version]",
    description: "Download the source code for Firefox",
    action: (version: string) => {
        if(!version) {

        }

        const releaseUrl = `https://archive.mozilla.org/pub/firefox/releases/`
    }
}