import React from "react";
import { Section } from "../../../../shared/components/Section";

export const ThemeEditor = () => {
    return (
        <Section title={"Appearance"} breadcrumbs={["Themes", "Edit Theme"]}>

        </Section>
    );
}