import { observer } from "mobx-react";
import React from "react";

export const Spring = observer(() => {
    return (
        <div
            className={"chromeclass-toolbar-additional"}
        ></div>
    );
});