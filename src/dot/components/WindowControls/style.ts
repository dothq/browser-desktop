import styled from "styled-components";

export const WindowControlsContainer = styled.div`
    display: flex;
    padding: 0px 8px;
`;

export const MinimiseButton = styled.button`
    appearance: -moz-window-button-minimize;
`;

export const MaximiseButton = styled.button`
    appearance: -moz-window-button-maximize;
`;

export const CloseButton = styled.button`
    appearance: -moz-window-button-close;
`;